# Copia este archivo a .env y rellena tus claves (NUNCA subas .env al repo).
# En GitHub, estos van como Secrets del repositorio (Settings > Secrets > Actions).

GROQ_API_KEY=tu_clave_groq
JSON2VIDEO_API_KEY=tu_clave_json2video_plan_gratis

# Opcionales:
LANG_CODE=es
# TOPIC=Escribe aquí el tema del reel si quieres sobreescribir el de config.py
